name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  build-and-test:
    name: Build, test, fuzz-smoke, analyze
    runs-on: ubuntu-latest
    # GitHub Actions ubuntu-latest runners have inconsistent AVX-512
    # availability — some have it, some don't. We pin MARCH=x86-64-v3
    # (AVX2 baseline) for CI so the binaries run on every runner SKU.
    # Local builds still default to MARCH=x86-64-v4 (AVX-512); the
    # multi-versioned USM pool dispatches at runtime regardless of
    # build baseline, so AVX-512 codegen IS exercised in CI through
    # the variant .o files (test_usm_pool_variants gates per-CPU).
    env:
      MARCH: x86-64-v3
    steps:
      - uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential clang cppcheck pkg-config \
            libvlccore-dev libvlc-dev \
            libswscale-dev libavutil-dev

      - name: Show toolchain info
        run: make info

      - name: Build the VLC plugin
        run: make plugin

      - name: Unit tests (ASan + UBSan)
        run: make test

      - name: Smoke fuzz (100k iterations under ASan + UBSan)
        run: make fuzz-smoke

      - name: Concurrency stress (usm_pool, ASan + TSan, ~70s)
        run: make stress

      - name: Line coverage (gcov, fails any tracked file < 80%)
        run: make coverage

      - name: Static analysis (cppcheck)
        run: make analyze

  libfuzzer:
    name: libFuzzer (60s each)
    runs-on: ubuntu-latest
    env:
      MARCH: x86-64-v3
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential clang

      - name: Build fuzzers
        run: make fuzz

      - name: Run libFuzzer (upscale_logic) for 60 seconds
        run: |
          mkdir -p corpus_logic
          if [ -d tests/corpus ]; then cp tests/corpus/* corpus_logic/ 2>/dev/null || true; fi
          build/fuzz_upscale_logic corpus_logic -max_total_time=60 -print_final_stats=1

      - name: Run libFuzzer (usm) for 60 seconds
        run: |
          mkdir -p corpus_usm
          build/fuzz_usm corpus_usm -max_total_time=60 -max_len=64 -print_final_stats=1

      - name: Run libFuzzer (frame_shape) for 60 seconds
        run: |
          mkdir -p corpus_frame_shape
          if [ -d tests/corpus_frame_shape ]; then cp tests/corpus_frame_shape/* corpus_frame_shape/ 2>/dev/null || true; fi
          build/fuzz_frame_shape corpus_frame_shape -max_total_time=60 -max_len=24 -print_final_stats=1

      - name: Run libFuzzer (scaler_chroma) for 60 seconds
        run: |
          mkdir -p corpus_scaler_chroma
          if [ -d tests/corpus_scaler_chroma ]; then cp tests/corpus_scaler_chroma/* corpus_scaler_chroma/ 2>/dev/null || true; fi
          build/fuzz_scaler_chroma corpus_scaler_chroma -max_total_time=60 -max_len=8 -print_final_stats=1

      - name: Run libFuzzer (content_probe) for 60 seconds
        run: |
          mkdir -p corpus_content_probe
          build/fuzz_content_probe corpus_content_probe -max_total_time=60 -max_len=64 -print_final_stats=1

      - name: Upload findings
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: fuzzer-findings
          path: |
            crash-*
            timeout-*
            leak-*

  sonarcloud:
    name: SonarCloud (C/C++ scan, tests excluded)
    runs-on: ubuntu-latest
    env:
      MARCH: x86-64-v3
    steps:
      - uses: actions/checkout@v4
        with:
          # Sonar needs full history for blame / new-code detection.
          fetch-depth: 0

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential clang pkg-config \
            libvlccore-dev libvlc-dev \
            libswscale-dev libavutil-dev

      - name: Install sonar build-wrapper
        uses: SonarSource/sonarcloud-github-c-cpp@v3

      - name: Build plugin under build-wrapper
        # Build the production target only — tests are not invoked.
        # build-wrapper captures every compiler invocation into
        # bw-output/ so the sonar-scanner can analyse the C/C++ TUs.
        run: |
          build-wrapper-linux-x86-64 --out-dir bw-output make plugin

      - name: Run sonar-scanner
        uses: SonarSource/sonarcloud-github-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
